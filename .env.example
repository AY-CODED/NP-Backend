# Database
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/nuges_pharmacy
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=change-me

# Use a random value containing at least 32 bytes.
JWT_SECRET=
JWT_EXPIRATION_MS=604800000

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Firebase Admin JSON should be configured in the deployment platform, not committed.
# Existing Np-Admin Firebase project:
FIREBASE_SERVICE_ACCOUNT_JSON=
# Customer authentication Firebase project (nugescus):
FIREBASE_CUSTOMER_SERVICE_ACCOUNT_JSON=

# Deployed frontends
APP_FRONTEND_CUSTOMER_URL=https://nugesphramacy.vercel.app
APP_FRONTEND_ADMIN_URL=https://np-admin-one.vercel.app
APP_CORS_ALLOWED_ORIGINS=https://nugesphramacy.vercel.app,https://np-admin-one.vercel.app

# Cross-site HTTPS cookie settings for Vercel -> Render.
AUTH_COOKIE_NAME=jwt
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_SAME_SITE=None
AUTH_COOKIE_MAX_AGE_SECONDS=604800

# Checkout and Flutterwave (keep secret values only on the backend host)
CHECKOUT_SHIPPING_COST=1500
CHECKOUT_VAT_RATE=0.075
CHECKOUT_CURRENCY=NGN
CHECKOUT_PAYMENT_EXPIRY_MINUTES=30
CHECKOUT_RESERVATION_GRACE_MINUTES=10
FLUTTERWAVE_SECRET_KEY=
FLUTTERWAVE_WEBHOOK_SECRET_HASH=
FLUTTERWAVE_MAX_RETRY_ATTEMPTS=3

# Private prescription upload limit (5 MB by default)
PRESCRIPTION_MAX_FILE_SIZE_BYTES=5242880

# Transactional email. Configure an SMTP provider before enabling.
APP_MAIL_ENABLED=false
MAIL_FROM=no-reply@nugespharmacy.com
MAIL_BRAND_NAME=Nuges Pharmacy
EMAIL_VERIFICATION_REQUIRED=false
EMAIL_VERIFICATION_TTL_MINUTES=1440
PASSWORD_RESET_TTL_MINUTES=30
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_AUTH=true
SMTP_STARTTLS=true
SMTP_STARTTLS_REQUIRED=true
